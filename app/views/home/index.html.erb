<div class="col-xs-12 col-md-8 col-lg-8">
					<% 5.times do |image| %>
	                    <div id="work" class="works">
	                        <%= link_to(image_tag(image)) %>
	                    </div>
                    <% end %>
                </div>
            </div>
        </div>